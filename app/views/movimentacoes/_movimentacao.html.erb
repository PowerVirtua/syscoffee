<div id="<%= dom_id movimentacao %>" class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0">Detalhes da Movimentação - Lote: <%= movimentacao.codigo_lote %></h5>
  </div>
  <div class="card-body">
    <p><strong>Tipo:</strong> <%= movimentacao.tipo.capitalize %></p>

    <p><strong>Cliente:</strong> <%= Cliente.find(movimentacao.cliente_id).nome %></p>

    <p><strong>Quantidade de Sacas:</strong> <%= movimentacao.quantidade_sacas %></p>

    <p><strong>Peso Total:</strong> <%= movimentacao.peso_total %> kg</p>

    <p><strong>Localização:</strong> <%= movimentacao.localizacao %></p>

    <p><strong>Qualidade:</strong> <%= Qualidade.find(movimentacao.qualidade_id).nome %></p>

    <p><strong>Data de Cadastro:</strong> <%= movimentacao.data_cadastro.strftime("%d/%m/%Y") %></p>

    <p><strong>Embalagem:</strong> <%= Embalagem.find(movimentacao.embalagem_id).nome %></p>

    <p><strong>Observação:</strong> <%= movimentacao.observacao %></p>
  </div>
</div>
